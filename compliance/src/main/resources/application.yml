spring:
  datasource:
    # url: jdbc:postgresql://192.168.62.94:5432/postgres
    url: jdbc:postgresql://10.201.2.93:5432/postgres
    username: postgres
    password: 'kbds1!'
    driver-class-name: org.postgresql.Driver
  jackson:
    time-zone: Asia/Seoul

logging:
  level:
    root: info                    # 📌 프로젝트 전체의 기본 로그를 터미널에 출력 (info 레벨)
    com.kbds.compliance: debug    # 📌 우리가 짠 코드 내부의 SQL 쿼리와 로그는 더 상세히(debug) 출력
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n" # 콘솔 포맷 지정

cors:
  allowed-origins:
    - http://localhost:3000
    - http://localhost:8501
    - http://10.201.2.93
    - http://192.168.62.94